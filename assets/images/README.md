# Site Images

Put portfolio images in this directory. Suggested filenames:

- `headshot.jpg` for the About section headshot
- `og-image.jpg` for social sharing metadata

Reference images from the root page with paths such as `assets/images/headshot.jpg`.
